# bedrock-agentcore[strands-agents] pulls in the AgentCore Memory session-manager
# integration for Strands (bedrock_agentcore.memory.integrations.strands).
bedrock-agentcore[strands-agents]>=1.19.0
strands-agents>=1.50.2
boto3>=1.43.62
numpy>=2.5.1
pandas>=3.0.5
# MCP client transport used to reach the AgentCore Gateway (streamable HTTP).
mcp>=2.0.0
# SigV4-signing MCP transport (aws_iam_streamablehttp_client) for IAM-authorized
# access to the AgentCore Gateway MCP endpoint. Ships with the strands-agents
# extra above; pinned explicitly so the import is guaranteed present.
mcp-proxy-for-aws>=1.6.4
